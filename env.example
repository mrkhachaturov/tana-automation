# =============================================================================
# TANA AUTOMATION CONFIGURATION
# =============================================================================
# Copy this file to .env: cp env.example .env

# =============================================================================
# AUTHENTICATION (OAuth - no passwords stored)
# =============================================================================
USER_DATA_DIR=./chrome-profile
BROWSER_CHANNEL=chrome

# =============================================================================
# KANBAN WATCHER CONFIGURATION (for docker-compose.watcher.yml)
# =============================================================================

# Your Tana Kanban board URL (required)
TANA_KANBAN_URL=https://app.tana.inc/?ampDeviceId=YOUR_ID&wsid=YOUR_WSID

# Column configuration - How to find column IDs:
#   1. Open Kanban in Chrome → Right-click column header → Inspect
#   2. Find: data-testid="column-XXXXX"
#
# Option 1: All columns in one line
COLUMNS_CONFIG=Backlog:column-XXXXX,In Progress:column-YYYYY,Done:column-ZZZZZ
#
# Option 2: Individual columns
# COLUMN_BACKLOG=column-XXXXX
# COLUMN_IN_PROGRESS=column-YYYYY
# COLUMN_DONE=column-ZZZZZ

# Button to click when status changes (default: "SYNC")
BUTTON_TEXT=SYNC
# Or use CSS selector: BUTTON_SELECTOR=[data-testid="sync-btn"]
BUTTON_SELECTOR=

# Polling interval in milliseconds (default: 2000)
POLL_INTERVAL=2000

# Webhook for notifications (Slack/Discord)
# WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
WEBHOOK_URL=

# =============================================================================
# CLI AUTOMATION (for npm run dev)
# =============================================================================

# Tana board links
TANA_LINKS=https://app.tana.inc/some/link

# Single card mode
TANA_TARGET_TEXT=Node title or unique text
TANA_STATUS_FIELD=Status
TANA_STATUS_VALUE=Done

# Column mode
# TANA_COLUMN=In progress
# TANA_COLUMN_TESTID=column-YnoDw59tQHaz

# =============================================================================
# RUNTIME
# =============================================================================
HEADLESS=0

